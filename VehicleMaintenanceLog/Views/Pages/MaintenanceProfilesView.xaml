<UserControl x:Class="VehicleMaintenanceLog.Views.MaintenanceProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VehicleMaintenanceLog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid HorizontalAlignment="Stretch" Grid.Column="0" Margin="10 10 0 10">
                <TextBlock Text="Maintenance Profile" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding MaintenanceProfiles}" SelectedItem="{Binding SelectedMaintenanceProfile}" DisplayMemberPath="ProfileName" VerticalAlignment="Center" VerticalContentAlignment="Center" Height="25" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
            </Grid>

            <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft" Grid.Column="1">
                <Button Content="Delete Profile" Command="{Binding DeleteSelectedProfileCommand}" Margin="5" Width="120"/>
                <Button Content="Edit Profile" Command="{Binding LoadEditProfileWindowCommand}" Margin="5" Width="120"/>
                <Button Content="New Profile" Command="{Binding LoadNewProfileWindowCommand}" Margin="5" Width="120"/>
            </StackPanel>
        </Grid>

        <ListView x:Name="lvProfileSchedules" ItemsSource="{Binding MaintenanceProfileSchedules}" SelectedItem="{Binding SelectedSchedule}" GridViewColumnHeader.Click="lvHeader_Click" Grid.Row="1" Margin="10" SelectionMode="Single" Background="#FFD6DADF">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Width="0" DisplayMemberBinding="{Binding ScheduleID}"/>
                        <GridViewColumn Header="Task" DisplayMemberBinding="{Binding TaskName}" Width="150"/>
                        <GridViewColumn Header="Mileage Increment" DisplayMemberBinding="{Binding MileageIncrementStr}" Width="110"/>
                        <GridViewColumn Header="Time Increment" DisplayMemberBinding="{Binding TimeIncrementStr}" Width="110"/>
                        <GridViewColumn Header="Notes" DisplayMemberBinding="{Binding ScheduleNotes}" Width="300"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>


            <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft" Grid.Column="0">
                <Button Content="Remove Task" Command="{Binding DeleteSelectedScheduleCommand}" Margin="5" Width="120"/>
                <Button Content="Edit Task Schedule" Command="{Binding LoadEditScheduleWindowCommand}" Margin="5" Width="120"/>
                <Button Content="Add Task" Command="{Binding LoadNewScheduleWindowCommand}" Margin="5" Width="120"/>
            </StackPanel>
        </Grid>



    </Grid>
</UserControl>
