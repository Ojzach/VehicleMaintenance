<Window x:Class="VehicleMaintenanceLog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VehicleMaintenanceLog"
        xmlns:vms="clr-namespace:VehicleMaintenanceLog.ViewModels"
        xmlns:views="clr-namespace:VehicleMaintenanceLog.Views"
        mc:Ignorable="d"
        Title="MainWindow" 
        MinHeight="350" MinWidth="700" Height="700" Width="1200">

    <DockPanel>

        <Grid DockPanel.Dock="Top" Height="25" Background="DimGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <Grid Width="200" DockPanel.Dock="Left">
            <Grid.Resources>
                <DataTemplate DataType="{x:Type vms:NavigationPanelViewModel}">
                    <views:NavigationPanelView/>
                </DataTemplate>
            </Grid.Resources>

            <ContentControl Content="{Binding CurrentNavigationPanelViewModel}"/>
        </Grid>

        <Grid>
            <Grid.Resources>
                <DataTemplate DataType="{x:Type vms:VehicleOverviewPageViewModel}">
                    <views:VehicleOverviewPageView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:VehiclesPageViewModel}">
                    <views:VehiclesPageView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:TasksPageViewModel}">
                    <views:TasksPageView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:MaintenanceProfilesPageViewModel}">
                    <views:MaintenanceProfilesView/>
                </DataTemplate>
            </Grid.Resources>

            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Grid>
    </DockPanel>


</Window>
