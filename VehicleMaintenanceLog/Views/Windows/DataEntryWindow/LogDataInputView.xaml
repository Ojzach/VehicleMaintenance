<UserControl x:Class="VehicleMaintenanceLog.Views.LogDataInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VehicleMaintenanceLog.Views"
             mc:Ignorable="d" >


    <StackPanel Grid.Row="1" MinWidth="350"  Orientation="Vertical" HorizontalAlignment="Stretch">

        <Grid Margin="5">
            <TextBlock Text="Task Completed"/>
            <ComboBox x:Name="cbTask" Grid.Column="2" ItemsSource="{Binding Tasks}" DisplayMemberPath="TaskName" SelectedItem="{Binding SelectedTask}" HorizontalAlignment="Right" MaxWidth="200" MinWidth="100"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Mileage" Margin="0, 0, 85, 0"/>
            <TextBox Text="{Binding MileageInput}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Date Completed" Margin="0, 0, 40, 0"/>
            <DatePicker SelectedDate="{Binding DateInput}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Temp Fix" Margin="0, 0, 40, 0"/>
            <CheckBox IsChecked="{Binding IsTempFixInput}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Visibility="{Binding TempFixInputVisibility}" Margin="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Grid Width="150" Margin="10, 0, 0, 0">
                <TextBlock Text="Miles"/>
                <TextBox Text="{Binding TempFixMileageInput}" Width="100"  HorizontalAlignment="Right"/>
            </Grid>

            <Grid Width="180" Margin="10, 0, 0, 0">
                <TextBlock Text="Time (Months)" HorizontalAlignment="Left"/>
                <TextBox Text="{Binding TempFixTimeInput}" Width="100" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>

        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Notes" HorizontalAlignment="Stretch"/>
            <TextBox Text="{Binding NotesInput}"  Grid.Column="1" Height="100" HorizontalAlignment="Stretch"/>
        </Grid>

    </StackPanel>
</UserControl>
