<UserControl x:Class="VehicleMaintenanceLog.Views.Windows.DataEntryWindow.MaintenanceProfileDataInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VehicleMaintenanceLog.Views.Windows.DataEntryWindow"
             mc:Ignorable="d" d:DesignWidth="258.162" >
    
    <StackPanel Grid.Row="1" Orientation="Vertical">

        <Grid Margin="5">
            <TextBlock Text="Profile Name"/>
            <TextBox Text="{Binding NewProfile.ProfileName, UpdateSourceTrigger=PropertyChanged}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Vehicle Type Constraint"/>
            <ComboBox ItemsSource="{Binding VehicleTypes}" MaxWidth="200" MinWidth="100" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </Grid>


    </StackPanel>
</UserControl>
