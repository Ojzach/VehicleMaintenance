<UserControl x:Class="VehicleMaintenanceLog.Views.Windows.DataEntryWindow.VehicleDataInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VehicleMaintenanceLog.Views.Windows.DataEntryWindow"
             mc:Ignorable="d" >
    
    
    <StackPanel Grid.Row="1" MinWidth="225" Orientation="Vertical" HorizontalAlignment="Stretch">

        <Grid Margin="5">
            <TextBlock Text="Vehicle Name" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding NewVehicle.VehicleName}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Vehicle Type" HorizontalAlignment="Left"/>
            <ComboBox ItemsSource="{Binding VehicleTypes}" SelectedItem="{Binding NewVehicle.VehicleType}" MaxWidth="200" MinWidth="100" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Vehicles Mileage" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding NewVehicle.VehicleMileage}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Manufacture Date" HorizontalAlignment="Left"/>
            <DatePicker SelectedDate="{Binding NewVehicle.VehicleManufactureDate, Mode=TwoWay, StringFormat='d'}" MaxWidth="200" MinWidth="100" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Maintenance Profile" HorizontalAlignment="Left"/>
            <ComboBox ItemsSource="{Binding MaintenanceProfiles}" SelectedItem="{Binding NewVehicle.VehicleMaintenanceProfile}" DisplayMemberPath="ProfileName" MaxWidth="200" MinWidth="100" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </Grid>



    </StackPanel>
</UserControl>
