<UserControl x:Class="VehicleMaintenanceLog.Views.Windows.DataEntryWindow.TaskScheduleDataInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VehicleMaintenanceLog.Views.Windows.DataEntryWindow"
             mc:Ignorable="d" d:DesignHeight="209.707" d:DesignWidth="302.995">

    <StackPanel Grid.Row="1" Orientation="Vertical">

        <Grid Margin="5">

            <TextBlock Text="Task"/>
            <ComboBox ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" DisplayMemberPath="TaskName" HorizontalAlignment="Right" MaxWidth="200" MinWidth="100"/>

        </Grid>

        <Grid Margin="5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition MaxWidth="200" MinWidth="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Mileage Increment" Grid.Column="0"/>
            <CheckBox IsChecked="{Binding MileageIncrementEnabled}" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox Text="{Binding NewSchedule.MileageIncrement}" IsEnabled="{Binding MileageIncrementEnabled}" Grid.Column="2" HorizontalAlignment="Stretch"/>
        </Grid>

        <Grid Margin="5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition MaxWidth="200" MinWidth="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Time Increment (Months)" Grid.Column="0"/>
            <CheckBox IsChecked="{Binding TimeIncrementEnabled}" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox Text="{Binding NewSchedule.TimeIncrement}" IsEnabled="{Binding TimeIncrementEnabled}" Grid.Column="2" HorizontalAlignment="Stretch"/>
        </Grid>

        <Grid Margin="5">
            <TextBlock Text="Schedule Notes"/>
            <TextBox Text="{Binding NewSchedule.ScheduleNotes}" MinWidth="200" Height="60" HorizontalAlignment="Right"/>
        </Grid>

    </StackPanel>
</UserControl>
